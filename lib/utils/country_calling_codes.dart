// currently this class only contain calling code for kenya, later we might add other calling codes.
class CountryCallingCode {
  // make this class non-instantiable
  CountryCallingCode._();

  static int kenya = 254;
}
